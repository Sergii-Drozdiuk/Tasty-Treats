const BASE_URL = 'https://tasty-treats-backend.p.goit.global/api';

const selectTime = document.querySelector('.select-time');
const selectArea = document.querySelector('.select-area');

const searchIngredients = document.querySelector('.select-ingredients');
export { selectTime, searchIngredients, BASE_URL, selectArea };
